[pytest]

# Le dossier backend contient directement le package "app".
# On l'ajoute au chemin Python pendant les tests.
pythonpath = .

# Les tests officiels de ComptaFlow se trouvent ici.
testpaths = tests

# Affichage utile des tests ignorés / xfail / erreurs.
addopts = -ra